[pytest]
# `python -m pytest` puts the working directory on sys.path; the bare `pytest`
# console script does not, so `import pipeline` fails under one invocation and
# not the other. Setting it here makes both work, which matters because the
# Makefile uses one form and anyone typing `pytest` by hand uses the other.
pythonpath = .
testpaths = tests
